// import {
//   getEnhancedEventsFromTx,
//   extractOnChainData,
// } from "@/events-sync/handlers/royalties/utils";
import { jest, describe, it } from "@jest/globals";

jest.setTimeout(1000 * 1000);

describe("Polygon Opensea ZeroExv3", () => {
  it("parse-event", async () => {
    // const results = await getEnhancedEventsFromTx(
    //   "0x572c41b0fcedb63e2c6a732b57ae66a096da74fe227bfa3b1ec351730dfb3446"
    // );
    // console.log("results", results);
  });
});
